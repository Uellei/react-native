import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "flex-start",
    paddingTop: 60
  },
  image: {
    borderRadius: 100,
    borderWidth: 1,
    borderColor: "black",
    width: 200,
    height: 200,
    overflow: "hidden"
  },
  text: {
    paddingTop: 10,
    fontSize: 40,
  }
});