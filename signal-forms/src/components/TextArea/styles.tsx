import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingLeft: 10,
    lineHeight: 20,
    borderWidth: 3,
    borderColor: "#33333",
    borderRadius: 7,
    height: 120
  }
});