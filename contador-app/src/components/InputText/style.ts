import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
  },
  textInputContainer: {
    borderWidth: .5,
    borderColor: "#fff",
    borderRadius: 5,
    marginTop: 25
  },
  textInput: {
    color: "#fff",
    fontSize: 20,
    padding: 10,
    // fontWeight: "200"
    fontFamily: "Montserrat-Light"
  }
});